h2 Edit item #{item.name}
form(action=`/items/${item.id}/edit?_method=PATCH` method="POST")
  label(for="new-item") Change to:
  input(type="text" name="new-item")
  input(type="submit" value="Submit")
